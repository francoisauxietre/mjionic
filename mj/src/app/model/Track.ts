// classe metier
// pour la fenetre musique : un chanson (nom et chemein de sauvegarde de celle-ci
// pour une utlisation avec firebase mettre l'url dans path

export class Track {
    name: string;
    path: string;
}
