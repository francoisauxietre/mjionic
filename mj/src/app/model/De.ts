export class De {
    // constructor(deValue: number, deQuantity: number) {
    //     this.deValue = deValue;
    //     this.quantity = deQuantity;
    // }

    deValue: number;
    quantity: number;
}
